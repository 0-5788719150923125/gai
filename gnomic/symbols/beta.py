from alpha import Alpha as α

class Beta(α):
    def init(self):
        self.symbol = 'β'

    def is_exclusive():
        """
        A symbol must define exactly one new class method.
        We somehow link this to testing.
        """
        return True